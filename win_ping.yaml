---
- name: Check Windows Connectivity
  hosts: all
  gather_facts: no

  tasks:

    - name: Test WinRM connectivity
      win_ping:
      register: ping_result

    - name: Show result
      debug:
        var: ping_result