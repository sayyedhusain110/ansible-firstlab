---
- name: Check Windows Connectivity
  hosts: aut-win01
  gather_facts: no
  become: no

  tasks:

    - name: Test WinRM connectivity
      win_ping:
      register: ping_result

    - name: Show result
      debug:
        var: ping_result